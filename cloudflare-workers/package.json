{
  "name": "axim-core-cloudflare-workers",
  "version": "1.0.0",
  "engines": {
    "node": ">=22.0.0"
  },
  "description": "Cloudflare Workers for AXiM Core Edge Network Integration",
  "main": "src/index.js",
  "scripts": {
    "build": "rm -rf ../.wrangler && npx wrangler deploy --dry-run --outdir=dist",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test:integration": "vitest run tests/integration.test.js",
    "test": "npm run test:integration"
  },
  "devDependencies": {
    "vitest": "^2.1.8",
    "wrangler": "^4.95.0"
  }
}