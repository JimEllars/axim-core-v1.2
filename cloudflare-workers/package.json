{
  "name": "axim-core-cloudflare-workers",
  "version": "1.0.0",
  "engines": {
    "node": ">=22.0.0"
  },
  "description": "Cloudflare Workers for AXiM Core Edge Network Integration",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "npx wrangler deploy --dry-run -c wrangler.toml",
    "dry-run": "npx wrangler deploy --dry-run -c wrangler.toml",
    "check": "npm run test && npm run dry-run",
    "tail": "wrangler tail",
    "test:integration": "vitest run tests/integration.test.js",
    "build": "npx wrangler deploy --dry-run -c wrangler.toml",
    "test": "npm run test:integration",
    "deploy:dry-run": "npx wrangler deploy --dry-run -c wrangler.toml"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@vitejs/plugin-react": "^6.1.1",
    "@vitest/coverage-v8": "^2.1.9",
    "eslint": "^10.11.0",
    "jsdom": "^29.1.1",
    "vitest": "^2.1.9",
    "wrangler": "^4.95.0"
  },
  "type": "module"
}