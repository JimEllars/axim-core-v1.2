From b18c218515020b38f5d5ccac585f7931b65294d5 Mon Sep 17 00:00:00 2001
From: "google-labs-jules[bot]"
 <161369871+google-labs-jules[bot]@users.noreply.github.com>
Date: Fri, 28 Aug 2026 19:46:05 +0000
Subject: [PATCH 1/2] Harden Department Dispatcher Validation

---
 supabase/functions/universal-dispatcher/index.ts | 6 +++++-
 1 file changed, 5 insertions(+), 1 deletion(-)

diff --git a/supabase/functions/universal-dispatcher/index.ts b/supabase/functions/universal-dispatcher/index.ts
index 1ec9c16..3f4c336 100644
--- a/supabase/functions/universal-dispatcher/index.ts
+++ b/supabase/functions/universal-dispatcher/index.ts
@@ -77,7 +77,11 @@ serve(async (req: Request) => {

     const { action_type, payload } = body;

-    const target_department = body.target_department || payload?.target_department || 'CORE';
+    let target_department = body?.target_department ?? payload?.target_department;
+    if (target_department === null || target_department === undefined || typeof target_department !== 'string') {
+        target_department = 'CORE';
+    }
+
     const VALID_DEPARTMENTS = ['CEO', 'CFO', 'COO', 'CORE'];
     if (!VALID_DEPARTMENTS.includes(target_department)) {
         return new Response(
--
2.53.0


From 889be99356840d25f8cd67ec437e2d5d5d1e9775 Mon Sep 17 00:00:00 2001
From: "google-labs-jules[bot]"
 <161369871+google-labs-jules[bot]@users.noreply.github.com>
Date: Fri, 28 Aug 2026 19:48:04 +0000
Subject: [PATCH 2/2] Update AGENTS_VERIFICATION.md for Dispatcher Hardening

---
 AGENTS_VERIFICATION.md                           | 7 +++++++
 supabase/functions/universal-dispatcher/index.ts | 2 +-
 2 files changed, 8 insertions(+), 1 deletion(-)

diff --git a/AGENTS_VERIFICATION.md b/AGENTS_VERIFICATION.md
index b6cf65c..d23f109 100644
--- a/AGENTS_VERIFICATION.md
+++ b/AGENTS_VERIFICATION.md
@@ -54,3 +54,10 @@
 * Verified routes \`/admin/cfo\` are fully registered in \`App.jsx\`.
 * Verified \`Sidebar.jsx\` includes the new 'CFO Dashboard' link and icon.
 * Created backing SQL RPC function \`get_cfo_pending_approvals\` in a new migration to fulfill the data fetch.
+
+## Wave 140: Dispatcher Hardening
+* Implemented \`VALID_DEPARTMENTS\` array \`['CEO', 'CFO', 'COO', 'CORE']\` in \`universal-dispatcher/index.ts\`.
+* Hardened department extraction to safely fallback to \`'CORE'\` if invalid, null, or undefined.
+* Returns 400 Bad Request immediately if the provided department string is not within the valid range.
+* Verified High Stakes \`toolCalledPayload\` correctly serializes \`target_department\`.
+* Ran \`scripts/test-department-dispatch.cjs\` locally to ensure target department is successfully serialized.
diff --git a/supabase/functions/universal-dispatcher/index.ts b/supabase/functions/universal-dispatcher/index.ts
index 3f4c336..bf872b6 100644
--- a/supabase/functions/universal-dispatcher/index.ts
+++ b/supabase/functions/universal-dispatcher/index.ts
@@ -322,7 +322,7 @@ serve(async (req: Request) => {

       const { error: jobError } = await supabaseAdmin.from("satellite_job_queue").insert({
         app_id: "universal-dispatcher",
-        payload: { action_type, payload },
+        payload: { action_type, payload: { ...payload, target_department } },
         status: 'pending',
         task_type: action_type
       });
--
2.53.0
