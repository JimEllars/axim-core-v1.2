--- src/components/admin/KPIOverview.jsx
+++ src/components/admin/KPIOverview.jsx
@@ -128,7 +128,8 @@
       {info && <span className="text-xs text-slate-500 mt-1">{info}</span>}
     </div>
   </div>
-);
+  );
+};

 const KPIOverview = () => {
   const { supabase } = useSupabase();
